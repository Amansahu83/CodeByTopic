class Solution {
public:
    bool doesValidArrayExist(vector<int>& derived) {
        int countOnes=0;
        for(int i=0;i<derived.size();i++)
        {
            if(derived[i])countOnes++;

        }
        if(countOnes%2)return 0;
        return 1;
    }
};
